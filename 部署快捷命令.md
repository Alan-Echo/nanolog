# NanoLog 部署快捷命令

## 环境信息

| 项目 | 值 |
|---|---|
| 镜像 | `ghcr.io/alan-echo/nanolog:1.1.1` |
| 容器名 | `nanolog-server` |
| 端口 | `8080` |
| 数据目录 | `G:\Workspace\Alan-Ark\nanolog-data` |
| Web 控制台 | `http://localhost:8080` |

---

## 运行

```bash
# 首次启动（如果容器不存在）
docker run -d \
  -p 8080:8080 \
  -v "G:\Workspace\Alan-Ark\nanolog-data:/root/data" \
  --name nanolog-server \
  ghcr.io/alan-echo/nanolog:1.1.1

# 查看日志
docker logs -f nanolog-server
```

## 停止 & 启动

```bash
# 停止
docker stop nanolog-server

# 启动
docker start nanolog-server

# 重启
docker restart nanolog-server
```

## 升级

```bash
# 拉取新版本镜像（例：1.1.2）
docker pull ghcr.io/alan-echo/nanolog:1.1.2

# 停旧容器
docker stop nanolog-server

# 删旧容器（数据在物理卷，不会丢）
docker rm nanolog-server

# 启动新容器（更新镜像 tag）
docker run -d \
  -p 8080:8080 \
  -v "G:\Workspace\Alan-Ark\nanolog-data:/root/data" \
  --name nanolog-server \
  ghcr.io/alan-echo/nanolog:1.1.2
```

## 备份 & 恢复

```bash
# 备份（停容器后复制整个数据目录）
docker stop nanolog-server
cp -r "G:\Workspace\Alan-Ark\nanolog-data" "G:\Workspace\Alan-Ark\nanolog-data-backup-$(date +%Y%m%d)"
docker start nanolog-server

# 恢复（停容器 → 替换数据目录 → 启动）
docker stop nanolog-server
rm -rf "G:\Workspace\Alan-Ark\nanolog-data"
cp -r "G:\Workspace\Alan-Ark\nanolog-data-backup-20250101" "G:\Workspace\Alan-Ark\nanolog-data"
docker start nanolog-server
```

## 常用排查

```bash
# 查看容器状态
docker ps -a --filter name=nanolog-server

# 查看实时日志
docker logs -f --tail=100 nanolog-server

# 进入容器内部
docker exec -it nanolog-server sh

# 查看容器资源占用
docker stats nanolog-server

# 检查数据目录内容
ls -la "G:\Workspace\Alan-Ark\nanolog-data"
```

## 数据目录文件说明

| 文件 | 用途 |
|---|---|
| `.nanolog.key` | 主密钥 — **务必备份，丢失后数据不可恢复** |
| `.nanolog.sys` | 加密元数据（用户、Token、配置） |
| `log_*.nano` | 列式日志数据文件 |
| `wal.log` | 预写日志（未刷盘的增量数据） |
| `stats.json` | 持久化统计信息 |
